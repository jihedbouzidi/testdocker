version: '3'
services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules        # évite conflit node_modules local
    command: npm run dev
